[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Netlify serves the site from the domain root, unlike GitHub Pages
  # which needs the /trackd/ subpath.
  BASE_PATH = "/"
